# Origin

Cloned from `ExperienceQuality/xq-versastacks` module `modules/xq-ios-act-cli` at commit `44654adca7a1e86d38d5636c35df59fb09aebd6c`, then renamed to **`xq-motest`** (binary, package, skill, env vars).

This CLI talks **directly to DeviceKit** (JSON-RPC). It does not use the `agent-device` Node daemon stack.
